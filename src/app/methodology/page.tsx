import Link from 'next/link';
export default function MethodologyPage() { return <section className="page-shell"><div className="eyebrow"><i /> METHOD / 001</div><h1>THE COURT<br /><em>SHOWS ITS WORK.</em></h1><div className="method-grid"><article><span className="number">01</span><h2>Acquire safely</h2><p>Public URLs are fetched server-side with HTTP(S) validation, reserved-address DNS checks, bounded redirects, robots directives, an eight-second whole-response timeout and a 2 MB response limit. These application-level checks do not provide complete DNS-rebinding protection.</p></article><article><span className="number">02</span><h2>Classify deterministically</h2><p>Every intake is classified as VIDEO, NEWS_POST, PAPER, TRADER_PERSONA, ASSET or UNKNOWN before optional analysis. PDFs retain metadata and a hash only: text extraction and PDF-derived claims are NON_AVAILABLE.</p></article><article><span className="number">03</span><h2>Quote or abstain</h2><p>If GEMINI_API_KEY is absent, no claims are generated. If enabled, each accepted claim has a non-empty statement, exactly one known evidence ID, and exact non-empty quotations from that evidence item.</p></article><article><span className="number">04</span><h2>Conservative gates</h2><p>Verdicts are WATCH or VETO while evidence is incomplete. PAPER_TEST is disabled in this MVP. Public guest cases are read-only and shareable by link; do not submit confidential material.</p></article></div><Link className="outline-button" href="/">← RETURN TO INTAKE</Link></section>; }
